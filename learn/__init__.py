"""learn/ -- a real, CPU-only self-play reinforcement learner for hide-and-seek
(tabular Q-learning). The small, runs-anywhere counterpart to the GPU JAX/MAPPO
stack: the agents' behaviour is LEARNED through self-play, not scripted."""
