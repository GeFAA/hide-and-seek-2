"""Runnable examples for Hide & Seek 2.0.

This package holds small, self-contained demo scripts (e.g. ``quickstart.py``).
It deliberately contains no library logic — examples import from the top-level
``config`` / ``envs`` / ``models`` / ``trainers`` / ``utils`` packages and are
meant to be run from the repository root with ``PYTHONPATH=.``.
"""
